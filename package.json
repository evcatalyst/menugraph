{
  "name": "menugraph",
  "version": "0.1.0",
  "private": true,
  "description": "Exploratory visualization interface for the CIA historical menu collection.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build:data": "node scripts/build-static-data.js --text=25 --prices=180",
    "test": "node --check server.js && node --check docs/ontology.js && node --check docs/price-utils.js && node --check docs/archive-client.js && node --check docs/app.js && node scripts/test-price-utils.js"
  },
  "engines": {
    "node": ">=20"
  }
}
